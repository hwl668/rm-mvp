#pragma once

namespace rm
{
    // Color definitions
    enum Color
    {
        BLUE = 0,
        RED = 2
    };

    // Armor type definitions
    enum ArmorType
    {
        UNKNOWN_ARMOR = -1,
        SMALL_ARMOR = 0,
        BIG_ARMOR = 1
    };
}